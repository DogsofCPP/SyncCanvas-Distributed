# SyncCanvas 离线重连真实端测试报告

测试日期：2026-06-03  
测试对象：SyncCanvas-Distributed WebSocket 网关、前端 `StrokeCollector`、Redis/Kafka/MongoDB 持久化链路  
测试目标：验证断网/服务中断后，客户端是否按合理频率重连，本地显示数据与服务端持久化数据是否一致，以及是否出现大量重复请求。

## 一、测试环境

本次测试使用项目真实依赖栈和真实 HTTP/WebSocket 接口执行。

| 组件 | 状态 | 说明 |
| --- | --- | --- |
| Redis | 正常 | `localhost:6379` 可达 |
| MongoDB | 正常 | `localhost:27017` 可达 |
| Zookeeper | 正常 | Docker Compose 健康 |
| Kafka | 正常 | `localhost:9092` 可达 |
| SyncCanvas 网关 | 正常 | `http://localhost:3000` 可达 |

启动过程中发现默认 `node` 为 v14.16.1，无法运行当前 MongoDB 依赖，报错为 `Unexpected token '??='`。测试改用本机新版 Node：

```text
D:\Program Files\nodejs\node.exe
v24.16.0
```

网关服务启动后，日志显示 Redis、MongoDB、Kafka Producer 和 Kafka Consumer 均连接成功。

## 二、测试方法

本次执行两类真实端验证。

1. 服务端历史补齐验证

   - 注册测试用户 A、B。
   - 用户 A 打开测试画布并断开 WebSocket。
   - 用户 B 在同一画布写入一条真实笔画。
   - 等待 Kafka Consumer 将笔画持久化到 MongoDB。
   - 用户 A 重新连接 WebSocket。
   - 检查服务端是否通过 `sync_response` 下发断线期间产生的历史操作。

2. 前端离线缓存与自动重连验证

   - 用户 A 通过真实 `StrokeCollector` 建立 WebSocket 连接。
   - 停止 SyncCanvas 网关，模拟服务中断。
   - 在离线期间通过 Collector 发送一条本地笔画，使其进入离线缓存。
   - 观察重连频率与重试次数。
   - 重启 SyncCanvas 网关。
   - 检查 Collector 是否自动连回，并检查离线笔画是否 flush 到服务端 MongoDB。

## 三、测试数据

测试画布：

```text
canvas-75c925518198
```

断线期间由用户 B 写入的远端笔画：

```text
remote-during-offline-1780469581796-jjw0qi
```

Collector 离线期间产生的本地笔画：

```text
collector-offline-1780469581796-jjw0qi
```

MongoDB 查询结果：

```json
{
  "canvasId": "canvas-75c925518198",
  "totalDocsForCanvas": 1,
  "remoteStrokeCount": 1,
  "offlineCollectorStrokeCount": 0,
  "docs": [
    {
      "stroke_id": "remote-during-offline-1780469581796-jjw0qi",
      "point_count": 2,
      "sequence_id": 26248,
      "user_id": "rt_b_1780469581796-jjw0qi"
    }
  ]
}
```

## 四、测试结果

### 1. 服务端历史补齐通过

用户 A 断开期间，用户 B 写入的笔画成功进入服务端链路：

- WebSocket 网关收到用户 B 的 `stroke` 消息。
- Redis Pub/Sub 广播成功。
- Kafka Consumer 批量保存成功。
- MongoDB 中存在该笔画，`remoteStrokeCount = 1`。
- 用户 A 重连后，服务端从 Redis 历史缓存命中 1 条操作，并下发 `sync_response`。

结论：服务端“断线后重连拉取历史”的能力可用。

### 2. 前端 Collector 自动重连失败

模拟网关不可达后，Collector 第一次重连失败即停止后续重连。日志中出现：

```text
[Collector] WebSocket 错误: ECONNREFUSED
[Collector] 重连次数已达上限
```

服务恢复后，Collector 没有再次自动连接，离线缓存中的笔画也没有同步到服务端。MongoDB 中：

```text
offlineCollectorStrokeCount = 0
```

结论：前端自动重连链路失败，离线期间本地操作会与服务端数据不一致。

### 3. 未发现大量重复请求

服务中断和恢复期间，服务端日志中未观察到大量重复 WebSocket 连接或请求风暴。

但该结果不能视为重连策略健康，因为实际原因是 Collector 过早停止重连，而不是合理限频后持续尝试。

## 五、问题定位

主要问题位于 `public/collector.js` 的 WebSocket 连接逻辑。

`connect(url)` 每次调用都会执行：

```js
this.maxReconnectAttempts = 0;
```

断线后的 `_attemptReconnect()` 会再次调用 `connect(this.wsUrl)`。因此第一次重连尝试失败后，`maxReconnectAttempts` 已被重置为 `0`，后续 `_attemptReconnect()` 判断：

```js
if (this.reconnectAttempts >= this.maxReconnectAttempts) {
  console.error('[Collector] 重连次数已达上限');
  return;
}
```

此时 `reconnectAttempts = 1`，`maxReconnectAttempts = 0`，所以立即停止重连。

此外，`draw.js` 中部分图形类操作直接调用 `collector.send(element)`，断线时不会进入 `_sendSegment()` 的离线缓存逻辑，存在离线操作直接丢失风险。

## 六、结论

本次真实端测试结论如下：

| 检查项 | 结果 |
| --- | --- |
| 断线期间服务端写入是否可被重连客户端补齐 | 通过 |
| 本地离线操作恢复后是否同步到服务端 | 不通过 |
| 断网后是否停止无意义重试 | 部分通过，但原因异常 |
| 恢复后是否按合理频率重连 | 不通过 |
| 是否出现大量重复请求 | 未发现 |
| 本地显示数据与服务端数据是否一致 | 不一致 |

最终结论：当前版本服务端历史同步能力基本可用，但前端自动重连和离线缓存 flush 存在缺陷。断网恢复后，客户端不能可靠自动重连，离线期间本地显示的数据可能无法同步到服务端，导致本地与服务端数据不一致。

## 七、建议修复方向

1. 区分“主动断开”和“异常断线”

   `disconnect()` 应标记为主动关闭，主动关闭时不重连；异常断线时允许重连。

2. 不要在每次 `connect()` 中重置最大重连次数

   `maxReconnectAttempts` 应保持配置值，重连成功后再重置 `reconnectAttempts`。

3. 使用合理退避策略

   建议使用指数退避或线性退避，并设置最大间隔，例如 1s、2s、4s、8s、10s，避免频繁请求。

4. 统一离线发送入口

   图形、文字、自由笔画等所有可持久化操作都应通过同一个离线缓存发送方法，避免 `collector.send()` 直接失败后丢数据。

5. 重连成功后执行一次历史同步

   重连成功后应依赖服务端 `sync_response` 补齐断线期间其他用户产生的操作，并 flush 本地离线缓存。需要注意去重，避免本地乐观渲染与服务端回放重复显示。
